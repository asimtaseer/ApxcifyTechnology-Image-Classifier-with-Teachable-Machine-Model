# ApxcifyTechnology-Image-Classifier-with-Teachable-Machine-Model
This project demonstrates an image classifier built using Google Teachable Machine. The model is trained on a few custom classes (e.g., happy, sad, neutral) and leverages a pre-trained deep learning model internally, so no model is built from scratch.
